# Inventory System Backend

This is the backend application for the Inventory System, built with FastAPI.

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Run the application: `uvicorn main:app --host 0.0.0.0 --port 8000`

Ensure you have a PostgreSQL database running and update `DATABASE_URL` in `.env.example`.